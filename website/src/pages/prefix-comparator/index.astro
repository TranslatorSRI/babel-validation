---
import Layout from '../../layouts/Layout.astro';
import PrefixComparator from '../../components/PrefixComparator.vue';
---
<Layout title="Babel Validation: Prefix Comparator" description="Compare two prefix comparator results.">
    <main>
        <small><a href=".">Return to front page</a></small>

        <h1>Prefix Comparator</h1>
        <p>
            Babel produces a prefix report that tells you how many CURIE prefixes are found underneath each
            clique leader prefix. This tool allows you to compare two prefix reports to quickly understand
            how two Babel outputs differ.
        </p>

        <PrefixComparator client:only="vue" />
    </main>
</Layout>
