
.type curie = symbol
.type synonym = symbol

.type SynonymList = [
	item: synonym,
	next: SynonymList
]
.type CurieList= [
	item: curie,
	next: CurieList
]

.decl Synonym(
	curie: curie,
	preferred_name: synonym
)

// Read synonyms from a GZipped synonyms file.
.input Synonym(IO=file, filename="../data/2023may18/synonyms/AnatomicalEntity.tsv")

.decl result(curie: curie, label: synonym)
result(curie, preferred_name) :- Synonym(curie, preferred_name), match(".*cartilage.*", preferred_name).

.output result(IO=stdout)
